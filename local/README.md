# Local Config

Your personal shell settings — PATH, aliases, directory shortcuts.

These are separate from the shell machinery so they're easy to find and edit.

| File | What it does |
|------|-------------|
| `path.zsh` | PATH and fpath exports (Homebrew, local bins, completions) |
| `aliases.zsh` | Personal aliases and shortcuts |
